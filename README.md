# FavorAccess - Landing Page

A simple, modern one-page website (HTML/CSS/JS). No build step.

## Run locally
Just open `index.html` in your browser.

## Deploy
- GitHub Pages: push to a repo, then enable Pages (Settings → Pages)
- Netlify: drag-and-drop the folder or connect the repo
- Vercel: deploy as a static site

## Customize
- Update brand text in `index.html`
- Change colors in `styles.css` under `:root`
- Replace service descriptions and contact email
